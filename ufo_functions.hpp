#include <vector>
#include <string>

void display_misses(int misses);
void greet();
void endgame(std::string answer, std::string codeword);
void display_status(std::vector<char>, std::string answer);
